#include "Texture.h"

using namespace RTE;

void Texture::Draw(Box source, Box dest, glm::vec2 origin, float angle, Color color) {
	bool flipX = false;
	bool flipY = false;

	if (source.GetHeight() < 0) {

	}
}
